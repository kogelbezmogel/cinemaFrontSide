import {Box, Typography} from "@mui/material";

const Footer = () => {
    return (
        <Box sx={{
            width : '100%',
            padding : 1,
            backgroundColor : 'primary.main',
            color : 'white',
            margin : 0,
        }}>
           <Typography variant="h6" component="a">
               Footer 2023 expert level please hire me for senior react-jakarta-springframework developer.
           </Typography>
        </Box>
    );
}

export default Footer;